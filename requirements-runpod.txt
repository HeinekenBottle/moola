# Moola RunPod GPU Training Requirements
# Template: runpod/pytorch:2.4-py3.11-cuda12.4-ubuntu22.04
# Pre-installed in template: torch, torchvision, numpy, cuda, scipy, scikit-learn
# Last Updated: 2025-10-17
#
# Installation:
#   pip install --no-cache-dir -r requirements-runpod.txt
#
# Expected install time: ~60-90 seconds
# Expected size: ~50MB

#############################################
# Core ML Libraries (NOT in RunPod template)
#############################################
# Data handling (NOT in template)
pandas>=2.3,<3.0

# Gradient boosting (NOT in template)
xgboost>=2.0,<3.0

# SMOTE for class imbalance (NOT in template)
imbalanced-learn==0.14.0

#############################################
# PyTorch Ecosystem
#############################################
# Advanced training utilities (NOT in template)
pytorch-lightning>=2.4.0,<3.0

#############################################
# Data Handling & Validation
#############################################
# Parquet file support (NOT in template)
pyarrow>=17.0,<18.0

# Schema validation (NOT in template)
pandera>=0.26.1,<1.0

#############################################
# Configuration Management
#############################################
# CLI framework (NOT in template)
click>=8.2,<9.0
typer>=0.17,<1.0

# Config system (NOT in template)
hydra-core>=1.3,<2.0

# Data validation and settings (NOT in template)
pydantic>=2.11,<3.0
pydantic-settings>=2.9,<3.0

# Environment variables (NOT in template)
python-dotenv>=1.0

#############################################
# Logging & Output
#############################################
# Structured logging (NOT in template)
loguru>=0.7,<1.0

# Rich terminal output (NOT in template)
rich>=14.0,<15.0

#############################################
# Utilities
#############################################
# Job scheduling (NOT in template)
joblib>=1.5,<2.0

#############################################
# YAML Support
#############################################
PyYAML>=6.0

#############################################
# NOTES
#############################################
# DO NOT ADD: torch, torchvision, numpy, scipy, scikit-learn (already in template)
# DO NOT ADD: Development tools (pytest, black, ruff, etc.)
# DO NOT ADD: API/deployment packages (fastapi, uvicorn, etc.)
# DO NOT ADD: MLOps tools (mlflow, etc.) - use local logging instead
#
# Verification commands:
#   python -c "import torch, numpy, pandas; print('✅ Core imports work')"
#   python -c "import xgboost, pytorch_lightning; print('✅ ML libs work')"
#   du -sh /tmp/moola-venv  # Should be < 200MB
