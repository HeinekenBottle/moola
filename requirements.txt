# Moola Core Dependencies - Paper-Strict Pinned Versions
# This file contains only essential dependencies for core functionality
# Development tools are in pyproject.toml [project.optional-dependencies.dev]

#############################################
# Core ML & Data Libraries (Paper-Strict)
#############################################
# numpy 1.26.4: Keeping in 1.26.x series (NumPy 2.0+ has breaking changes)
# pandas 2.3.3: Upgraded from 2.2.2 (backward compatible, minor improvements)
# scipy 1.16.1: Upgraded from 1.11.4 (backward compatible for moola usage)
# scikit-learn 1.7.2: Upgraded from 1.5.2 (no breaking changes for moola models)
numpy==1.26.4
pandas==2.3.3
scipy==1.16.1
scikit-learn==1.7.2



#############################################
# PyTorch Ecosystem (RunPod 2025 Optimized)
#############################################
# PyTorch 2.4.1 with CUDA 12.4 (RunPod standard for 2025)
# Breaking changes reviewed: moola codebase uses no deprecated APIs
# - Uses standard state_dict/load_state_dict (not deprecated)
# - Uses no device-specific autocast (torch.amp compatible)
# - No FSDP, DeviceMesh, or distributed.pipeline usage
# See analysis: requires.txt update rationale document
torch==2.4.1+cu124; sys_platform != 'darwin'
torch==2.4.1; sys_platform == 'darwin'
torchvision==0.19.1+cu124; sys_platform != 'darwin'
torchvision==0.19.1; sys_platform == 'darwin'

# Advanced training utilities (Lightning 2.4 supports PyTorch 2.4+)
pytorch-lightning==2.4.0
torchmetrics==1.8.2

#############################################
# Data Handling & Validation (Paper-Strict)
#############################################
# Parquet file support
# pyarrow 17.0.0: Upgraded from 16.1.0 (backward compatible, bug fixes for parquet)
# Note: GCS-specific parquet.read_table issue reported, but pd.read_parquet works fine
pyarrow==17.0.0

# Schema validation
pandera==0.26.1

# Technical analysis library for ATR/EMA calculations
# Note: Currently NOT installed/used in moola - can be removed if unused
# TA-Lib==0.4.28

#############################################
# Configuration Management
#############################################
# CLI framework
click==8.1.7
typer==0.17.1

# Config system
hydra-core==1.3.2

# Data validation and settings
pydantic==2.11.2
pydantic-settings==2.9.1

# Environment variables
python-dotenv==1.0.0

#############################################
# Logging & Output
#############################################
# Structured logging
loguru==0.7.2

# Rich terminal output
rich==14.0.0

# Visualization
matplotlib==3.9.0

#############################################
# GitOps & Deployment
#############################################
# System monitoring
psutil==6.1.0

#############################################
# Utilities
#############################################
# YAML support
PyYAML==6.0.1

# Parallel processing
joblib==1.4.2

# Progress bars
tqdm==4.66.1

#############################################
# Project Dependencies
#############################################
# This should be installed as editable: pip install -e .
# The following are kept for reference but normally installed via pyproject.toml
# -e git+https://github.com/HeinekenBottle/moola.git@main#egg=moola
