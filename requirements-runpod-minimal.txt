# Moola RunPod GPU Training Requirements - MINIMAL VERSION
# Template: runpod/pytorch:2.4-py3.11-cuda12.4-ubuntu22.04
# Last Updated: 2025-10-16
#
# ⚠️ CRITICAL: This file EXCLUDES packages already in the RunPod template
# ============================================================================
# Packages EXCLUDED (already in template, DO NOT reinstall):
#   - torch, torchvision, torchaudio (PyTorch 2.4+)
#   - numpy (1.26+)
#   - pandas (2.2+)
#   - scipy (1.14+)
#   - scikit-learn (1.3+)
#
# Why this matters:
#   - Installing conflicting versions triggers pip to compile from source
#   - Compiling scipy/pandas/sklearn takes 20+ minutes EACH
#   - Total waste: 45-60 minutes of GPU time + money
#   - This file saves you from that disaster
#
# Verification:
#   1. Run verify-template.sh BEFORE setup
#   2. If template missing packages, TERMINATE pod immediately
#   3. Select correct PyTorch template with full scientific stack
#
# Installation:
#   pip install --no-cache-dir -r requirements-runpod-minimal.txt

#############################################
# Core ML Libraries (NOT in RunPod template)
#############################################
# Gradient boosting (NOT in template)
xgboost>=2.0,<3.0

# SMOTE for class imbalance (NOT in template)
imbalanced-learn==0.14.0

#############################################
# PyTorch Ecosystem
#############################################
# Advanced training utilities (NOT in template)
pytorch-lightning>=2.4.0,<3.0

#############################################
# Data Handling & Validation
#############################################
# Parquet file support (NOT in template)
pyarrow>=17.0,<18.0

# Schema validation (NOT in template)
pandera>=0.26.1,<1.0

#############################################
# Configuration Management
#############################################
# CLI framework (NOT in template)
click>=8.2,<9.0
typer>=0.17,<1.0

# Config system (NOT in template)
hydra-core>=1.3,<2.0

# Data validation and settings (NOT in template)
pydantic>=2.11,<3.0
pydantic-settings>=2.9,<3.0

# Environment variables (NOT in template)
python-dotenv>=1.0

#############################################
# Logging & Output
#############################################
# Structured logging (NOT in template)
loguru>=0.7,<1.0

# Rich terminal output (NOT in template)
rich>=14.0,<15.0

#############################################
# MLOps & Experiment Tracking
#############################################
# Experiment tracking (NOT in template)
mlflow>=2.0,<3.0

#############################################
# Utilities
#############################################
# Job scheduling (NOT in template)
joblib>=1.5,<2.0

#############################################
# NOTES
#############################################
# Installation time: ~60-90 seconds
# Package size: ~50MB
# Total savings: 45+ minutes, 4GB space
#
# DO NOT ADD: torch, numpy, pandas, scipy, scikit-learn
# DO NOT ADD: Development tools (pytest, black, etc.)
# DO NOT ADD: API/deployment packages (fastapi, uvicorn, etc.)
#
# For debugging:
#   python -c "import torch, numpy, pandas, scipy, sklearn; print('✅ Template OK')"
